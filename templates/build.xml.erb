<?xml version="1.0"?>
<project name="<%= @project_name%>" default="default">
  <description>
    <%= @project_name.capitalize%> automation suite
  </description>
  <target name="default">
    <echo message="Running default target"/>
  </target>
</project>
